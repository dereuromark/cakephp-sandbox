<!DOCTYPE html>
<html>
<head>
	{{ helper_Html_charset()|raw }}

	<title>
		{{ __('myTwigExample') }}
		{{ this.fetch('title')|raw }}
	</title>

	{{ helper_Html_meta('icon')|raw }}

	{{ helper_Html_script('app')|raw }}

	{{ this.fetch('meta')|raw }}
	{{ this.fetch('css')|raw }}
	{{ this.fetch('script')|raw }}
</head>
<body>
<header>
	{{ this.fetch('header')|raw }}
</header>

{{ helper_Flash_render()|raw }}

<section>

	<h1>{{ this.fetch('title')|raw }}</h1>

	{{ this.fetch('content')|raw }}
</section>

<footer>
	{{ this.fetch('footer')|raw }}
</footer>
</body>
</html>
