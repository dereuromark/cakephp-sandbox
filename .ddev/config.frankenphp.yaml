#ddev-generated
webserver_type: generic
web_extra_daemons:
  - name: "frankenphp"
    command: "frankenphp run --config /etc/frankenphp/Caddyfile --adapter=caddyfile"
    directory: /var/www/html
  # Queue worker moved to cron (.ddev/web-build/queue.cron): a fresh, flock-guarded,
  # --max-runtime-bounded process per minute instead of a supervisor restart-loop.
web_extra_exposed_ports:
  - name: "frankenphp"
    container_port: 80
    http_port: 80
    https_port: 443
